-- Стадия воронки, переименованная агентством.
--
-- Имя стадии из сида на экране не показывается: оно английское, а
-- показывается перевод по коду — иначе грузинский агент видел бы «In base»
-- посреди грузинского интерфейса. Но как только агентство назвало стадию
-- по-своему, побеждать обязано его имя, иначе переименование не видно.
--
-- Значение по умолчанию `false`: всё, что уже лежит в базе, создано сидом.
ALTER TABLE "pipeline_statuses" ADD COLUMN     "nameIsCustom" BOOLEAN NOT NULL DEFAULT false;
