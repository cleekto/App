import type { DeepPartial, Dictionary } from '../types';

/**
 * Грузинский. Равноправный язык продукта, а не «дополнительный» (ADR-0008).
 *
 * Неполон на фазе 2, и это нормально: словарь наполняется вместе с интерфейсом.
 * Ненормально было бы скрыть неполноту — например, скопировав сюда английский
 * текст. Тогда никто никогда не узнал бы, что именно не переведено.
 *
 * Отсутствующий ключ виден на экране как `⟦ключ⟧` и попадает в отчёт покрытия.
 *
 * ВНИМАНИЕ при переводе: у мхедрули нет заглавных букв. Не пытайся передать
 * акцент регистром — в интерфейсе `text-transform: uppercase` запрещён.
 */
export const ka: DeepPartial<Dictionary> = {
  app: {
    name: 'Cleekto',
    tagline: 'უძრავი ქონების CRM',
  },
  common: {
    loading: 'იტვირთება',
    cancel: 'გაუქმება',
    save: 'შენახვა',
  },
};
