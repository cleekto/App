/**
 * Адреса сервера.
 *
 * Это КОНСТАНТЫ СБОРКИ: `build.mjs` подставляет их значения через `define`,
 * и в собранном файле от них остаются строковые литералы.
 *
 * Не `process.env`: в расширении нет процесса Node, и объявлять его типы
 * означало бы обещать компилятору окружение, которого в браузере нет.
 * Отдельные имена с подчёркиваниями видно сразу — их нельзя спутать
 * с переменной, которую можно поменять во время работы.
 *
 * Секретов здесь нет и быть не может: всё, что попало в бандл расширения,
 * публично по определению (правило 8).
 */

declare const __CLEEKTO_API_URL__: string;
declare const __CLEEKTO_APP_URL__: string;

/** Куда ходит service worker. */
export const API_URL: string = __CLEEKTO_API_URL__;

/** Куда ведёт ссылка «Открыть в Cleekto». */
export const APP_URL: string = __CLEEKTO_APP_URL__;
