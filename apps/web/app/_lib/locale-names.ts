import type { Locale } from '@kleekto/i18n';

/**
 * Названия языков — каждое на своём языке.
 *
 * НЕ ПЕРЕВОДЯТСЯ. Человек ищет глазами «ქართული», а не «грузинский»,
 * на каком бы языке ни был остальной интерфейс. Тот же приём, что
 * в переключателе языка.
 *
 * Список общий: он нужен и полям имени стадии на доске, и тексту
 * объявления на карточке объекта. Две копии одного списка разошлись бы
 * при первой же правке.
 */
export const LOCALE_NAMES: Record<Locale, string> = {
  ka: 'ქართული',
  en: 'English',
  ru: 'Русский',
};
